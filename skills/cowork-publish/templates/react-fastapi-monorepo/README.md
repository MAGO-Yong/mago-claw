# Cowork react-fastapi-monorepo scaffold

按 ai-demo-platform-guard-transform-skill / react-fastapi-monorepo profile 规范产物。

```
backend/     # FastAPI 后端（API + 静态托管前端 dist）
frontend/    # Vite + React SPA
prepack.sh   # cowork pack 自动跑：先 build 前端，再打 zip
```
